numbers_primes=[]
for number in range(2,100):
   isprime = True
   for i in range(2,number):
       if number%i ==0:
           isprime = False 
        
    
   if isprime:
       numbers_primes.append(number)
print(f'the prime numbers beetwen 1 and 100 is :', numbers_primes)